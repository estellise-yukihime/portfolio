name: release.yml

on:
  release:
    types: [released]

jobs:
  fetch:
    runs-on: desperation
    outputs:
      rebuild: ${{ steps.download.outcome == 'failure' }}
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v6
      - name: login registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: download image
        id: download
        continue-on-error: true
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: |
          VERSION=${TAG#v}
          docker buildx imagetools inspect ghcr.io/${{ github.repository }}-ui:$VERSION

          if [[ $? -ne 0 ]]; then
            echo "::error failed to download image"
            exit 1
          fi

  build:
    needs: fetch
    if: ${{ needs.fetch.outputs.rebuild == 'true' }}
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    uses: ./.github/workflows/prerelease.yml
    secrets: inherit

  deploy:
    needs: [fetch, build]
    if: ${{ always() && ( needs.fetch.result == 'success' && (needs.build.result == 'skipped' || needs.build.result == 'success')) }}
    runs-on: desperation
    steps:
      - uses: actions/checkout@v6
      - name: compose up
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
        run: |
          VERSION=${TAG_NAME#v}
          VERSION=$VERSION docker compose up -d
